# 服务端请求 baseUrl 模板（复制为 .env.<mode> 或 .env.<mode>.local 后按需修改）
#
# 代码经 src/config/api.ts 统一读取：import.meta.env.VITE_SERVER_BASE_URL
# - 留空：同源请求（本地 dev 走 Vite proxy；生产由后端同源托管前端产物）
# - 填写：跨域直连服务端 origin（结尾不带 /），如 https://worklist.example.com
#   需服务端放行 CORS 并允许 credentials（前端所有请求携带 cookie）
#
# 路径前缀约定（由代码维护，勿拼进该变量）：
# - /devops-api/*  运维平台接口（服务端反代 devops.vzan.com）
# - /api/*         应用自身接口（钉钉绑定等）
VITE_SERVER_BASE_URL=http://192.168.80.57:8080
