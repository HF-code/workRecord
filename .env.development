# 开发环境（npm run dev / npm run build:dev）
# 留空 = 同源请求，由 Vite dev proxy 转发到本地后端（见 vite.config.ts server.proxy，默认 http://localhost:8080）
# 如需绕过代理直连本地后端，可改为：VITE_SERVER_BASE_URL=http://localhost:8080
VITE_SERVER_BASE_URL=http://localhost:8080
