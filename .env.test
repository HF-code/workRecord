# 测试环境（npm run build:test）
# 前后端分离部署时，填写测试环境服务端完整 origin（结尾不带 /），例如：
#   VITE_SERVER_BASE_URL=https://worklist-test.example.com
# 注意：跨域直连时服务端需放行 CORS 且允许 credentials（前端请求带 cookie）
# 留空 = 同源请求（与生产相同的同源托管形态）
VITE_SERVER_BASE_URL=http://192.168.80.57:8080
